/**
 * The site renders on the server.
 *
 * Documentation that needs JavaScript to say anything is documentation a search
 * engine cannot read and a reader on a slow connection sees as a blank page —
 * and this is the one part of acyka where that matters, because being findable
 * is most of the job. The playground is the exception and asks for the browser
 * itself.
 */
export const prerender = false;
export const ssr = true;
